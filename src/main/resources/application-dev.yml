spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blogdemo?useUnicode=true&characterEncoding=utf-8&serverTimezone=CST
    username: root
    password: NLnl9697!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    com.blogdemo: debug
  file: log/blog-dev.log

